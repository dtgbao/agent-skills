interface:
  display_name: "Code Explore"
  short_description: "Deeply analyzes existing codebase features by tracing execution paths, mapping architecture layers, understanding patterns and abstractions, and documenting dependencies to inform new development"
  default_prompt: "Use $code-explore to trace this feature and document its architecture before we change it."
