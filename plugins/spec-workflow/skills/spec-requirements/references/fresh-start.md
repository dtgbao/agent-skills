# Mode: fresh Requirements-First start

There's no existing design or requirements for this spec — this is a
standard new feature request. Draft requirements directly from
`$ARGUMENTS` and any clarifying answers, following `template.md` for
structure and EARS rules. Nothing else is different about this mode; it's
the default case `template.md` assumes.

On approval, the next phase is `spec-design`.
