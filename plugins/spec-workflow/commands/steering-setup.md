---
description: Set up or refresh persistent project context (product/tech/structure) for the spec workflow.
argument-hint: "[what changed, e.g. 'we switched to Postgres']"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/steering-setup/SKILL.md` in full and follow
its instructions exactly, using the following as input: $ARGUMENTS
