---
description: Produce a design — derived from approved requirements, started fresh as Design-First, or as a bugfix root-cause/fix design. Auto-detects which mode applies.
argument-hint: "<feature-slug or new feature/design description> [notes or revision request]"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/spec-design/SKILL.md` in full and follow
its instructions exactly, using the following as input: $ARGUMENTS
